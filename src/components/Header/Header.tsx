import SearchBar from '../SearchBar/SearchBar'
import './Header.scss'

const Header = () => {
  return (
    <header className="header">
        <SearchBar/>
    </header>
  )
}

export default Header